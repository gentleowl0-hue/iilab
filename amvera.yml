meta:
  environment: node
  toolchain:
    name: npm
  version: "18"

build:
  skip: false
  additionalCommands:
    - npm ci
    - npm run build

artifacts:
  "*": /

run:
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: npx serve -s dist -l 80
  persistenceMount: /data
  containerPort: "80"