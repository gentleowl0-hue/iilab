meta:
  environment: node
  toolchain: npm
  version: 18

build:
  commands:
    - npm ci
    - npm run build

run:
  containerPort: 3000
  command: npx serve -s dist -l 3000

artifacts:
  - dist/